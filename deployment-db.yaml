apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-depl 
  labels:
    app: mysql8
spec:
  selector:
     matchLabels:
       app: mysql8
  strategy:
    type: Recreate 
  template:
    metadata:
      labels:
        app: mysql8
    spec:
      containers:
        - name: mysql-container
          image: devops_task-mysql
          imagePullPolicy: Never
          ports:
            - containerPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql8-service
spec:
  type: NodePort
  selector:
    app: mysql8
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
